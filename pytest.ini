[pytest]
testpaths = tests
# Puts the repo root on sys.path so `from tests.conftest import ...` resolves in a clean
# environment, in CI as well as locally.
pythonpath = .
addopts = -q --strict-markers --import-mode=importlib
